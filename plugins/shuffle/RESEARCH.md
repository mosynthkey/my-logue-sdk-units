# Shuffle research

Split from GlitchPad into a single-purpose NTS-3 FX. Same capture / Y-rate /
SYNC / HOLD behavior as the former multi-mode pad, always engaging this module.

## Preset / catalog overlap

Investigated adjacent experimental units. None are the same product, so they
are kept:

- No dedicated twin in the catalog.

GlitchPad itself is removed (replaced by Retrig / Reverse / Shuffle / Gater).
